export const db_host = String(process.env.PG_HOST);
export const db_port = Number(process.env.PG_PORT);
export const db_name = String(process.env.PG_DB_NAME);
export const db_user = String(process.env.PG_USER);
export const db_password = String(process.env.PG_PASSWORD);